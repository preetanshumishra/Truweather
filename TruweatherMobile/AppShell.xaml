<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TruweatherMobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:TruweatherMobile.Pages"
    Shell.FlyoutBehavior="Disabled">

    <!-- Auth Pages (no tabs) -->
    <ShellContent Route="login"
                  ContentTemplate="{DataTemplate pages:LoginPage}"
                  Shell.TabBarIsVisible="False" />

    <ShellContent Route="register"
                  ContentTemplate="{DataTemplate pages:RegisterPage}"
                  Shell.TabBarIsVisible="False" />

    <!-- Main App TabBar -->
    <TabBar Route="main">
        <ShellContent Route="dashboard"
                      Title="Dashboard"
                      ContentTemplate="{DataTemplate pages:DashboardPage}">
            <ShellContent.Icon>
                <FontImageSource
                    Glyph="🏠"
                    Size="24"
                    Color="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource White}}" />
            </ShellContent.Icon>
        </ShellContent>

        <ShellContent Route="locations"
                      Title="Locations"
                      ContentTemplate="{DataTemplate pages:LocationsPage}">
            <ShellContent.Icon>
                <FontImageSource
                    Glyph="📍"
                    Size="24"
                    Color="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource White}}" />
            </ShellContent.Icon>
        </ShellContent>

        <ShellContent Route="alerts"
                      Title="Alerts"
                      ContentTemplate="{DataTemplate pages:AlertsPage}">
            <ShellContent.Icon>
                <FontImageSource
                    Glyph="🔔"
                    Size="24"
                    Color="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource White}}" />
            </ShellContent.Icon>
        </ShellContent>

        <ShellContent Route="settings"
                      Title="Settings"
                      ContentTemplate="{DataTemplate pages:SettingsPage}">
            <ShellContent.Icon>
                <FontImageSource
                    Glyph="⚙️"
                    Size="24"
                    Color="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource White}}" />
            </ShellContent.Icon>
        </ShellContent>
    </TabBar>

</Shell>
